import { join } from "path";
export declare function resolveContainedPath(baseDir: string, target: string): string | null;
export declare function ensureContained(baseDir: string, target: string): string;
export { join };
