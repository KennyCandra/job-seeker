# Signal Platform

Bun monorepo for the Signal platform.

## Run

```bash
bun run dev
```

The API serves the dashboard frontend and exposes API routes from the same server.

- Dashboard: `http://localhost:3000`
- Health check: `http://localhost:3000/api/health`
